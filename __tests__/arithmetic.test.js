'use strict';

const arithmetic = require('../lib/arithmetic.js');
const faker = require('faker');

describe('Arithmetic Test', () => {
  
  it('Runs a math test', () => {
    let math = 4;
    expect(math).toEqual(4);
  });
});
